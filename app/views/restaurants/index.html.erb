<div class="container">
  <h1>Restaurants#index</h1>
</div>

<div class="container">
  <ul class="list-group">
    <% @restaurants.each do |r| %>
      <li class="list-group-item list-group-item-success" ><%= link_to r.name, restaurant_path(r)%></li>
    <% end %>
  </ul>
</div>
<br>
<div class="container">
  <%= link_to "Create a new restaurant",  new_restaurant_path, class: "btn btn-primary"%>
</div>
