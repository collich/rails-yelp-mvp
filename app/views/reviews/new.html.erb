<h1>Add a new Review for </h1>

<%= simple_form_for [@restaurant, @review] do |f| %>
  <%= f.input :content %>
  <%= f.input :rating, collection: 1..5 %>
  <%= f.submit %>
<% end %>
<br>

<%= link_to "Go back to link", restaurant_path(params[:restaurant_id]), class: "btn btn-primary" %>
